<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<HBox fx:id="rootContainer" alignment="CENTER_LEFT" spacing="15.0" xmlns="http://javafx.com/javafx/17.0.2-ea" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.yunhang.forum.controller.PostItemController">
  <padding>
    <Insets bottom="10.0" left="10.0" right="10.0" top="10.0" />
  </padding>

  <!-- 左侧：头像 -->
  <ImageView fx:id="avatarImageView" fitHeight="50.0" fitWidth="50.0" pickOnBounds="true" preserveRatio="true">
    <HBox.margin>
      <Insets left="5.0" />
    </HBox.margin>
  </ImageView>

  <!-- 中间：帖子内容 -->
  <VBox spacing="5.0" HBox.hgrow="ALWAYS">
    <!-- 标题 -->
    <Label fx:id="titleLabel" style="-fx-font-size: 16px; -fx-font-weight: bold;" text="帖子标题" wrapText="true" />

    <!-- 内容摘要 -->
    <Label fx:id="summaryLabel" style="-fx-font-size: 14px; -fx-text-fill: #666666;" text="帖子内容摘要..." wrapText="true" />

    <!-- 分类标签 -->
    <HBox fx:id="tagContainer" spacing="5.0">
      <Label fx:id="categoryLabel" text="分类" />
    </HBox>
  </VBox>

  <!-- 右侧：统计信息和时间 -->
  <VBox fx:id="rightContainer" alignment="CENTER_RIGHT" spacing="5.0">
    <!-- 时间 -->
    <Label fx:id="timeLabel" style="-fx-font-size: 12px; -fx-text-fill: #888888;" text="2小时前" />

    <!-- 统计信息 -->
    <VBox spacing="3.0">
      <Label fx:id="likesLabel" style="-fx-font-size: 12px; -fx-text-fill: #ff4757;" text="♥ 0" />
      <Label fx:id="commentsLabel" style="-fx-font-size: 12px; -fx-text-fill: #2ed573;" text="💬 0" />
      <Label fx:id="viewsLabel" style="-fx-font-size: 12px; -fx-text-fill: #3742fa;" text="👁 0" />
    </VBox>
  </VBox>
</HBox>
